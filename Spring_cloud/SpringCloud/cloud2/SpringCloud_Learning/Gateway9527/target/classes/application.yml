server:
  port: 9527


spring:
  application:
    name: gateway9527
  cloud:
    gateway:
      routes:
        - id: gateway_simple
          uri: http://www.ityouknow.com
          predicates:
            - Path=/spring-cloud

